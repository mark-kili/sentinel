[project]
name = "sentinel"
version = "0.1.0"
description = "Telegram-based server monitoring and maintenance platform"
readme = "README.md"
requires-python = ">=3.11"

dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"